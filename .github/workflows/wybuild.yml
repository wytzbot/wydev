name: WyBuild
on:
  workflow_dispatch:
    inputs:
      build_type:
        description: APK or AAB
        required: true
        default: apk
      build_mode:
        description: debug or release
        required: true
        default: debug
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate project
        run: |
          if [ ! -f ./gradlew ]; then
            echo "::error::No gradlew found. WyBuild currently requires a repository with a configured Android Gradle project."
            exit 1
          fi
          chmod +x ./gradlew
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - name: Build APK
        if: inputs.build_type == 'apk'
        run: ./gradlew assemble${{ inputs.build_mode == 'release' && 'Release' || 'Debug' }} --no-daemon
      - name: Build AAB
        if: inputs.build_type == 'aab'
        run: ./gradlew bundleRelease --no-daemon
      - name: Upload APK
        if: inputs.build_type == 'apk'
        uses: actions/upload-artifact@v4
        with:
          name: wybuild-apk
          path: '**/build/outputs/apk/**/*.apk'
          if-no-files-found: error
      - name: Upload AAB
        if: inputs.build_type == 'aab'
        uses: actions/upload-artifact@v4
        with:
          name: wybuild-aab
          path: '**/build/outputs/bundle/**/*.aab'
          if-no-files-found: error

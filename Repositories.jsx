export default function Repositories({repos,onOpen}){return <div className="page"><header><div><span className="eyebrow">GITHUB</span><h1>Repositories</h1></div></header><section className="panel">{repos.map(r=><button className="repoRow" key={r.id} onClick={()=>onOpen(r)}><span><b>{r.full_name}</b><small>{r.private?"Private":"Public"} · {r.default_branch}</small></span><span>›</span></button>)}</section></div>}
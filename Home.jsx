export default function Home({repos,onOpen}){return <div className="page"><header><div><span className="eyebrow">WORKSPACE</span><h1>Home</h1></div><button onClick={()=>onOpen()}>Open Repository</button></header><section className="panel"><h3>RECENT REPOSITORIES</h3>{repos.slice(0,8).map(r=><button className="repoRow" key={r.id} onClick={()=>onOpen(r)}><span><b>{r.full_name}</b><small>{r.private?"Private":"Public"} · {r.default_branch}</small></span><span>›</span></button>)}{!repos.length&&<p className="muted">No repositories loaded.</p>}</section></div>}